local null_ls = require("null-ls")

null_ls.setup({
    sources = {
        null_ls.builtins.formatting.isort,
        -- Optionally, add black or other formatters
        null_ls.builtins.formatting.black,
    },
})

